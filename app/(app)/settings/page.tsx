export default function SettingsPage() {
  return (
    <section className="view active" aria-label="Settings">
      <div className="page narrow">
        <header className="page-head">
          <h1>Settings</h1>
          <p className="page-sub">
            Connect an AI provider for the Advisor and Compliance Analyst. Reference search works without any setup.
          </p>
        </header>
        <p className="card-hint">Settings port in progress.</p>
      </div>
    </section>
  );
}
