export default function DashboardPage() {
  return (
    <section className="view active" aria-label="Dashboard">
      <div className="page">
        <header className="page-head">
          <h1>Good day</h1>
          <p className="page-sub">Your FX compliance workspace — limits, declarations and recent activity.</p>
        </header>
        <p className="card-hint">Dashboard port in progress.</p>
      </div>
    </section>
  );
}
